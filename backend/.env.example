# Port du backend (par défaut: 3000, configuré dans backend/config.js)
PORT=3000

# URL du frontend (par défaut: http://localhost:5112, configuré dans config.ts)
FRONTEND_URL=http://localhost:5112

# Environnement
NODE_ENV=development
